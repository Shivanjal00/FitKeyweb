// src/app/pricing/layout.tsx
export const metadata = {
  title: "Pricing",
  description:
    "Simple, honest pricing for gym and library passes — coming soon.",
};

export default function PricingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
