// src/app/about/layout.tsx
export const metadata = {
  title: "About",
  description:
    "Gymbym's mission is to make fitness and focused study spaces accessible without long-term contracts.",
};

export default function AboutLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
